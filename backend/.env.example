# Server
PORT=3000

# Database (PostgreSQL)
# Format: postgres://USER:PASSWORD@HOST:PORT/DB_NAME
DB_URI=postgres://USER:PASSWORD@HOST:5432/portfolio_web
DB_SSL=false

# Auth
JWT_SECRET=your_jwt_secret_here

# CORS (comma-separated origins)
CORS_ORIGIN=http://localhost:3001
